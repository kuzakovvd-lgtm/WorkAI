# Iteration: Phase 4.5 hardening (pre-flight before assess)

- Date: 2026-04-09
- Branch: `phase-4.5-hardening`
- Commit(s): `pending`
- Owner: `codex`
- Status: `completed`

## Scope

- Add PostgreSQL-backed integration job in CI.
- Add DLQ-style table `pipeline_errors` and normalize record-level error persistence.
- Add normalize single-flight concurrency safety via PostgreSQL advisory locks.
- Add assess-like EXPLAIN helper script and perf report location.

## Changes

- Migration:
  - `migrations/versions/0005_pipeline_errors.py`
- CI:
  - `.github/workflows/ci.yml` (`integration-postgres` job with postgres:16 service)
- Normalize:
  - `WorkAI/normalize/runner.py` (run_id, error limit, advisory lock, sheet+date refresh)
  - `WorkAI/normalize/queries.py` (delete by sheet+date)
- DB helpers:
  - `WorkAI/db/queries.py` (`PipelineErrorRecord`, `insert_pipeline_error`, `make_payload_excerpt`)
  - `WorkAI/db/__init__.py`
- Config/env:
  - `WorkAI/config/settings.py` (`max_errors_per_sheet`)
  - `.env.example`
  - `deploy/secrets.example/workai.env.example`
- Tests:
  - `tests/integration/test_normalize_hardening.py`
  - Integration marker updates in existing integration tests
  - `tests/unit/test_settings.py` normalize max-errors validator case
  - `pyproject.toml` pytest markers registration
- Perf:
  - `scripts/workai_explain_assess.py`
  - `docs/perf/assess_explain_2026-04-07.md`
- Docs:
  - `DB_CONTRACT.md`, `DECISIONS.md`, `RUNBOOK.md`, `README.md`, `ROADMAP.md`, `TASK_BOARD.md`

## Notes

- In this environment there is no available local PostgreSQL service, so online
  migration and integration execution must be validated in CI or a local DB.
