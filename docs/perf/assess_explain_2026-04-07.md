# Assess EXPLAIN baseline for 2026-04-07

- date: `2026-04-07`
- sheet_id: `ALL`
- status: `template`

This file is the baseline report location generated by:

```bash
python scripts/workai_explain_assess.py --date 2026-04-07
```

Run the command with `WORKAI_DB__DSN` pointing to a PostgreSQL database
where migrations are applied and `tasks_normalized` is populated.
